{
  "name": "cofre-token",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "start:server": "node server/server.js",
    "start:dev": "concurrently \"nodemon server/server.js\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "install": "npm install --prefix server && npm install --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
